@extends('layouts.master')@section('content')    <h1>Orden <a href="{{ url('orden/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') Orden</a></h1>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>                <tr>                    <th>@lang('form.sno')</th><th>Orden de Compra</th><th>Fecha de Compra</th><th>Actions</th>                </tr>            </thead>            <tbody>            @php $x=0; @endphp            @foreach($orden as $item)                @php $x++;@endphp                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url('orden', $item->id) }}">{{ $item->ordenCompra }}</a></td>                    <td>{{ $item->fecha_compra }}</td>                    <td>                        <a href="{{ url('orden/' . $item->id . '/edit') }}">                            <button type="submit" class="btn btn-primary btn-xs">@lang('form.update')</button>                        </a> /                        {!! Form::open([                            'method'=>'DELETE',                            'url' => ['orden', $item->id],                            'style' => 'display:inline'                        ]) !!}                            {!! Form::submit(trans('form.deletee'), ['class' => 'btn btn-danger btn-xs']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $orden->render() !!} </div>    </div>@endsection
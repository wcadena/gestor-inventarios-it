@extends('layouts.master')@section('content')    <!-- Full Table -->    <div class="block block-rounded block-bordered">        <div class="block-header block-header-default">            <h3 class="block-title">Puesto</h3>            <div class="block-options">                <button type="button" class="btn-block-option">                    <a href="{{ url('puesto/create') }}" class="btn btn-hero-primary js-click-ripple-enabled"><i class="si si-plus"></i> Puesto</a>                </button>            </div>        </div>        <div class="block-content">            <div class="table-responsive">                <table class="table table-bordered table-striped table-vcenter">                    <thead>                    <tr>                        <th>@lang('form.sno')</th><th>Código</th><th>Ubicación</th><th>Actions</th>                    </tr>                    </thead>                    <tbody>                    @php $x=0; @endphp                    @foreach($puesto as $item)                        @php $x++;@endphp                        <tr>                            <td>{{ $x }}</td>                            <td><a href="{{ url('puesto', $item->id) }}">{{ $item->codigo }}</a></td>                            <td>{{ $item->ubicacions['edificio'] }}</td>                            <td>                                <a href="{{ url('puesto/' . $item->id . '/edit') }}">                                    <button type="submit" class="btn btn-sm btn-light m-1">@lang('form.update')</button>                                </a> /                                {!! Form::open([                                    'method'=>'DELETE',                                    'url' => ['puesto', $item->id],                                    'style' => 'display:inline'                                ]) !!}                                {!! Form::button(__('form.deletee'), ['class' => 'btn btn-sm btn-light m-1','type' => 'submit']) !!}                                {!! Form::close() !!}                            </td>                        </tr>                    @endforeach                    </tbody>                </table>            </div>            <div class="pagination"> {!! $puesto->render() !!} </div>        </div>    </div>@endsection
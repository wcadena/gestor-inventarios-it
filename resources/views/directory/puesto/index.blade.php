@extends('layouts.master')@section('content')    <h1>Puesto <a href="{{ url('puesto/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') Orden</a></h1>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>            <tr>                <th>@lang('form.sno')</th><th>Código</th><th>Ubicación</th><th>Actions</th>            </tr>            </thead>            <tbody>            @php $x=0; @endphp            @foreach($puesto as $item)                @php $x++;@endphp                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url('puesto', $item->id) }}">{{ $item->codigo }}</a></td>                    <td>{{ $item->ubicacions['edificio'] }}</td>                    <td>                        <a href="{{ url('puesto/' . $item->id . '/edit') }}">                            <button type="submit" class="btn btn-primary btn-xs">@lang('form.update')</button>                        </a> /                        {!! Form::open([                            'method'=>'DELETE',                            'url' => ['puesto', $item->id],                            'style' => 'display:inline'                        ]) !!}                        {!! Form::submit(trans('form.deletee'), ['class' => 'btn btn-danger btn-xs']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $puesto->render() !!} </div>    </div>@endsection
@extends('layouts.master')@section('content')    @php        $route='roles';        $titulo='Roles';    @endphp    <h1>@lang('fo.'.$titulo)        <a href="{{ url($route.'/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') @lang('fo.Rol')</a>    </h1>    <div class="jumbotron">        <h1>Advertencia </h1>        <p>Los perfiles <b>no pueden editarse por esta consola</b>, si desea cambiarlas, se debe realizar por base de datos o eliminar y crear nuevo Rol,        tenga presente que al eliminar un rol, este afecta a los usuarios que no se actualicen los roles.</p>    </div>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>            <tr>                <th>@lang('form.sno')</th><th>Rol</th><th>@lang('fo.descripcion')</th><th>@lang('fo.Actions')</th>            </tr>            </thead>            <tbody>            @php $x=0; @endphp            @foreach($rol as $item)                @php $x++;@endphp                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url($route, $item->id) }}">{{ $item->display_name }}</a></td>                    <td>{{ $item->description }}</td>                    <td>                        {!! Form::open([                            'method'=>'DELETE',                            'url' => [$route, $item->id],                            'style' => 'display:inline'                        ]) !!}                        {!! Form::button(__('form.deletee'), ['class' => 'btn btn-sm btn-light m-1','type' => 'submit']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $rol->render() !!} </div>    </div>@endsection
@extends('layouts.master')@section('content')    <div class="block block-rounded block-bordered">        <div class="block-header block-header-default">            <h3 class="block-title">Checklist</h3>            <div class="block-options">                <button type="button" class="btn-block-option">                    <a href="{{ url('checklist/create') }}" class="btn btn-hero-primary js-click-ripple-enabled"><i class="si si-plus"></i> Checklist</a>                </button>            </div>        </div>        <div class="block-content">            <div class="table-responsive">                <table class="table table-bordered table-striped table-hover">                    <thead>                    <tr>                        <th>@lang('form.sno')</th><th>Area</th><th>User</th><th>Id Check Lists</th><th>@lang('form.equipos')</th><th>Actions</th>                    </tr>                    </thead>                    <tbody>                    @php $x=0; @endphp                    @foreach($checklist as $item)                        @php $x++;@endphp                        <tr>                            <td>{{ $x }}</td>                            <td><a href="{{ url('checklist', $item->id) }}">{{ $item->areaxc->area }}</a></td><td>{{ $item->userxc->name }}</td><td>{{ $item->id_check_lists }}</td>                            <td>{{ $item->equiposxm['no_serie']}}</td>                            <td>                                <a href="{{ url('checklist/' . $item->id . '/edit') }}">                                    <button type="submit" class="btn btn-sm btn-light m-1">@lang('form.update')</button>                                </a> /                                {!! Form::open([                                    'method'=>'DELETE',                                    'url' => ['checklist', $item->id],                                    'style' => 'display:inline'                                ]) !!}                                {!! Form::button(__('form.deletee'), ['class' => 'btn btn-sm btn-light m-1','type' => 'submit']) !!}                                {!! Form::close() !!}                            </td>                        </tr>                    @endforeach                    </tbody>                </table>                <div class="pagination"> {!! $checklist->render() !!} </div>            </div>        </div>    </div>@endsection
@extends('layouts.master')@section('content')    <h1>Checklist <a href="{{ url('checklist/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') Checklist</a></h1>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>                <tr>                    <th>@lang('form.sno')</th><th>Area</th><th>User</th><th>Id Check Lists</th><th>@lang('form.equipos')</th><th>Actions</th>                </tr>            </thead>            <tbody>            @php $x=0; @endphp            @foreach($checklist as $item)                @php $x++;@endphp                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url('checklist', $item->id) }}">{{ $item->areaxc->area }}</a></td><td>{{ $item->userxc->name }}</td><td>{{ $item->id_check_lists }}</td>                    <td>{{ $item->equiposxm['no_serie']}}</td>                    <td>                        <a href="{{ url('checklist/' . $item->id . '/edit') }}">                            <button type="submit" class="btn btn-primary btn-xs">@lang('form.update')</button>                        </a> /                        {!! Form::open([                            'method'=>'DELETE',                            'url' => ['checklist', $item->id],                            'style' => 'display:inline'                        ]) !!}                            {!! Form::submit(trans('form.deletee'), ['class' => 'btn btn-danger btn-xs']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $checklist->render() !!} </div>    </div>@endsection
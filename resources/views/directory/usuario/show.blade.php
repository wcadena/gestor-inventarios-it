@extends('layouts.backend')@section('main-content')    <h1>Usuario</h1>    <div class="table-responsive">        <table class="table table-bordered table-striped table-hover">            <thead>            <tr>                <th>ID.</th> <th>Name</th><th>First Name</th><th>Last Name</th>            </tr>            </thead>            <tbody>            <tr>                <td>{{ $usuario->id }}</td> <td> {{ $usuario->name }} </td><td> {{ $usuario->first_name }} </td><td> {{ $usuario->last_name }} </td>            </tr>            </tbody>        </table>    </div>    <div class="panel-group" id="accordion" role="tablist" aria-multiselectable="true">        <div class="panel panel-default">            @foreach( $usuario->roles as $rol)                <div class="panel-heading" role="tab" id="heading{{$rol->name}}">                    <h4 class="panel-title">                        <a role="button" data-toggle="collapse" data-parent="#accordion" href="#collapse{{$rol->name}}" aria-expanded="true" aria-controls="collapse{{$rol->name}}">                            {{$rol->display_name}}                        </a>                    </h4>                </div>                <div id="collapse{{$rol->name}}" class="panel-collapse collapse" role="tabpanel" aria-labelledby="heading{{$rol->name}}">                    <div class="panel-body">                        <ul class="list-group">                            @foreach($rol->permissions as $permiso)                                <li class="list-group-item" title="{{$permiso->description}}">{{$permiso->name}}|{{$permiso->display_name}}</li>                            @endforeach                        </ul>                    </div>                </div>            @endforeach        </div>    </div>@endsection
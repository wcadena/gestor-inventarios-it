@extends('layouts.app')@section('main-content')    <h1>Usuario <a href="{{ url('usuario/create') }}" class="btn btn-primary pull-right btn-sm">@lang('form.addnew') Usuario</a></h1>    <div class="table">        <table class="table table-bordered table-striped table-hover">            <thead>                <tr>                    <th>@lang('form.sno')</th><th>Name</th><th>First Name</th><th>Last Name</th><th>Actions</th>                </tr>            </thead>            <tbody>            @php $x=0; @endphp            @foreach($usuario as $item)                @php $x++;@endphp                <tr>                    <td>{{ $x }}</td>                    <td><a href="{{ url('usuario', $item->id) }}">{{ $item->name }}</a></td><td>{{ $item->first_name }}</td><td>{{ $item->last_name }}</td>                    <td>                        <a href="{{ url('usuario/' . $item->id . '/edit') }}">                            <button type="submit" class="btn btn-primary btn-xs">@lang('form.update')</button>                        </a> /                        {!! Form::open([                            'method'=>'DELETE',                            'url' => ['usuario', $item->id],                            'style' => 'display:inline'                        ]) !!}                            {!! Form::submit(trans('form.deletee'), ['class' => 'btn btn-danger btn-xs']) !!}                        {!! Form::close() !!}                    </td>                </tr>            @endforeach            </tbody>        </table>        <div class="pagination"> {!! $usuario->render() !!} </div>    </div>@endsection